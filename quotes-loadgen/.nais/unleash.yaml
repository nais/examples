apiVersion: unleash.nais.io/v1
kind: ApiToken
metadata:
  name: quotes-loadgen
  namespace: {{ namespace }}
  labels:
    team: {{ namespace }}
spec:
  unleashInstance:
    apiVersion: unleash.nais.io/v1
    kind: RemoteUnleash
    name: examples
  secretName: quotes-loadgen-unleash-api-token
  environment: development
