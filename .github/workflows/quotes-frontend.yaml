name: quotes-frontend

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/quotes-frontend.yaml
      - .github/workflows/service-pipeline.yaml
      - quotes-frontend/**
  pull_request:
    branches: [main]
    paths:
      - .github/workflows/quotes-frontend.yaml
      - .github/workflows/service-pipeline.yaml
      - quotes-frontend/**
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  pipeline:
    uses: ./.github/workflows/service-pipeline.yaml
    with:
      service-name: quotes-frontend
      service-path: ./quotes-frontend
      enable-tests: true
      enable-coverage: false
    secrets:
      NAIS_WORKLOAD_IDENTITY_PROVIDER: ${{ secrets.NAIS_IO_WORKLOAD_IDENTITY_PROVIDER }}
